name: Database migrations

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Dry-run first; deploy only after reviewing the plan
        required: true
        default: dry-run
        type: choice
        options: [dry-run, deploy]
      bootstrap_existing:
        description: Mark the already-installed Phase 1 migrations as applied
        required: true
        default: true
        type: boolean
      reconcile_phase2_to_5:
        description: Reapply Phase 2-5 when their migration history was marked applied after a rolled-back SQL attempt
        required: true
        default: false
        type: boolean

permissions:
  contents: read

concurrency:
  group: protstock-database-production
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Check required secrets
        run: |
          test -n "$SUPABASE_DB_URL" || (echo "Missing SUPABASE_DB_URL" && exit 1)
          test -n "$SUPABASE_DB_PASSWORD" || (echo "Missing SUPABASE_DB_PASSWORD" && exit 1)
      - name: Validate pooler URL password
        run: |
          python - <<'PY'
          import os
          import sys
          from urllib.parse import unquote, urlsplit

          parsed = urlsplit(os.environ["SUPABASE_DB_URL"])
          if not parsed.hostname or parsed.password is None:
              sys.exit("SUPABASE_DB_URL must be a complete PostgreSQL connection URL.")
          if unquote(parsed.password) != os.environ["SUPABASE_DB_PASSWORD"]:
              sys.exit("The password embedded in SUPABASE_DB_URL does not match SUPABASE_DB_PASSWORD.")
          print("Pooler URL password matches SUPABASE_DB_PASSWORD.")
          PY
      - name: Bootstrap migration history
        if: inputs.bootstrap_existing
        run: supabase migration repair 20260909010000 20260909020000 --status applied --db-url "$SUPABASE_DB_URL"
      - name: Reconcile rolled-back Phase 2-5 history
        if: inputs.reconcile_phase2_to_5
        run: supabase migration repair 20260909030000 20260909040000 20260909050000 20260909060000 --status reverted --db-url "$SUPABASE_DB_URL"
      - name: Review migration plan
        if: inputs.mode == 'dry-run'
        run: supabase db push --dry-run --include-all --db-url "$SUPABASE_DB_URL"
      - name: Apply migrations
        if: inputs.mode == 'deploy'
        run: supabase db push --include-all --db-url "$SUPABASE_DB_URL"
      - name: Verify migration status
        if: always() && inputs.mode == 'deploy'
        run: |
          supabase migration list --db-url "$SUPABASE_DB_URL"
          supabase db dump --db-url "$SUPABASE_DB_URL" --schema public --file remote_schema.sql
          grep -E '^CREATE TABLE public\.(technical_snapshots|support_resistance_zones|pattern_instances|pattern_points|fundamental_periods|fundamental_metrics|rules|rule_versions|signals|backtest_runs|backtest_trades|portfolios|positions|journal_entries)' remote_schema.sql || true
